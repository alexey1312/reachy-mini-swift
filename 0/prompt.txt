In the reachy-mini-swift repository, Tests/ReachyUITests/ConnectProgressModelTests.swift line ~183 has a timing assertion `#expect(elapsed < .seconds(1))` that failed on CI run 32862018240 (Lint & Test job, 3-core macos-15 runner) with elapsed = 1.47 s, on a PR that did not touch ConnectProgressModel or anything near it. Project rule 7 in CLAUDE.md says duration-as-assertion bounds must be loose enough that a loaded runner cannot cross them ("separating milliseconds from tens of seconds leaves room to spare"). Review every ContinuousClock bound in that file (there are several — the suite measures `dwell` budgets) and widen the failing one(s) so a loaded 3-core runner has real headroom, while keeping the bound meaningful against the branch it distinguishes (per RemoteControlChannelTests.expectTimeout's example). Run `./bin/mise run test:filter ConnectProgressModelTests` to verify.