{
  "name": "reachy-ci",
  "description": "What a runner gets: nothing kept. No job in ci.yml calls it today — slimming a GitHub-hosted runner's simulator cost more than it gave back (see the note above the smoke job) — but $CI selects this rather than dev.json wherever these tasks do run, including a self-hosted runner whose simulator keeps the state between jobs.",
  "except": [],
  "keep": []
}
